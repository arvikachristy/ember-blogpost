<h1>Here are my blog posts</h1>

<ul>{{#each model as |post|}}
	<li>{{post.title}}</li>
	by: {{post.author}}
<button {{action 'deletePost' post}}>Delete Post!</button>
	{{else}}
	Sorry, no post available :(
	{{/each}}
</ul>


{{outlet}}
